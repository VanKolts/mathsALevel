# Icon placeholders

Every emoji was removed from the app on **2026-08-17**. Each one left a placeholder naming
the icon that belongs there, so nothing has to be rediscovered when the real icons exist.

```html
<i class="ico" data-icon="camera" aria-hidden="true"></i>
```

**39 sites across 25 distinct icons** — 25 drawings is the whole job.

**Find them three ways**

| | |
|---|---|
| In the source | `grep -n 'data-icon' index.html` — and `grep -n "ico('" index.html` for the ones built in JS |
| In the console | `document.querySelectorAll('.ico')` |
| On screen | add `?icons` to the URL, or use test mode's **Icon slots** toggle — every placeholder outlines itself in place |

**Two kinds.** Most render as *nothing*, so the app looks finished today. A few marked
`ico-live` sit where the emoji **was** the control — the favourite heart, the note marker —
and draw a small neutral box instead, because rendering nothing would remove the affordance.
Those also carry `data-state="on|off"`.

**To replace one:** swap the `<i>` for your SVG. `ico(name, extra, state)` in `index.html`
is the single place the markup is generated for the JS-built ones.

> **Do not edit this file by hand — regenerate it:**
> ```
> node scripts/gen-icon-index.mjs
> ```
> Generated 2026-08-22 against a 8180-line `index.html`.
> It drifted for months before there was a generator, still listing two icons that had been
> deleted from the source.

---

## By icon

### `bolt`

- `index.html:232` — Quick log

### `book`

- `index.html:194` — Learning — for working through topics from scratch
- `index.html:340` — Glossary

### `calendar`

- `index.html:105` — Revision plan
- `index.html:1156` — Revision plan Exam dates per paper, and the plan they drive

### `camera`

- `index.html:233` — Detailed log
- `index.html:259` — Add a photo of the question
- `index.html:804` — 
- `index.html:1008` — Click or drop image
- `index.html:2427` — function tutorResetUploader(){ var up=document.getElementById('tutor-uploader'

### `celebrate`

- `index.html:911` — 
- `index.html:4294` — if(!focus.length) html+=' All caught up for this paper ';

### `chart`

- `index.html:342` — Performance

### `download`

- `index.html:666` — Pull cloud → this device

### `external`

- `index.html:378` — Worksheets on PMT

### `favourite`

- `index.html:5357` — const starBtn=document.createElement('button');starBtn.className='fav-star-btn
- `index.html:5385` — starBtn.innerHTML=ico('favourite','ico-live',isFav?'on':'off');

### `graduation`

- `index.html:343` — Grade Boundaries

### `note`

- `index.html:171` — A note you saved on a topic
- `index.html:5286` — const ni=document.createElement('span');ni.className='note-indicator';ni.inner

### `pin`

- `index.html:934` — 

### `pomodoro`

- `index.html:1157` — Focus timer Timed study sessions with your own presets

### `repeat`

- `index.html:196` — Revision — for going back through and solidifying topics
- `index.html:211` — Re-attempt this

### `resources`

- `index.html:104` — Resources
- `index.html:1155` — Resources Hand-picked sites for learning and practice

### `rocket`

- `index.html:945` — 

### `search`

- `index.html:518` — 

### `sparkle`

- `index.html:223` — Explain with AI
- `index.html:382` — Practice question
- `index.html:1158` — AI Tutor Photograph a question, get a worked walkthrough
- `index.html:4635` — body.innerHTML=' Generate a practice question ';

### `target`

- `index.html:4276` — if(focusPool.length) html+=' Today\'s focus '+focusPool.map(function(x){return

### `timer`

- `index.html:6425` — var tb=document.createElement('button');tb.className='pp-act timer';tb.innerHT

### `tip`

- `index.html:5785` — html+=' Log a past paper question-by-question to see exact marks lost. For now

### `upload`

- `index.html:665` — Push this device → cloud

### `warning`

- `index.html:6645` — note.innerHTML=' Per-question data isn\'t available for this paper yet. Switch

### `wave`

- `index.html:924` — 

### `wrench`

- `index.html:5783` — ' Your top 3 leaks alone are '+top3Lost+' marks — roughly '+jumps+' grade boun
